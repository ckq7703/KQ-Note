"""The app version: sent to the server with every request, so it can ask old clients to update.
Keep in step with installer.iss, version_info.txt and the release workflow (tests/test_version.py checks)."""

__version__ = "1.5.0"
